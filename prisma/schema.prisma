datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Cat {
  id          String  @id @default(uuid())
  name        String
  age         Int
  image       String
  price       Int
  description String
  color       String
  count       Int
}