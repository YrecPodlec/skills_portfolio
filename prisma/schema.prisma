generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Cat {
  id          String  @id @default(uuid())
  name        String
  age         Int
  image       String
  price       Int
  description String
  color       String
  count       Int
}